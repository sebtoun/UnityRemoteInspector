<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" href="favicon.ico" />
    <title>Unity Remote Inspector</title>
</head>
<body>
    <div>
        <h1>
            GameObject: <a href="/">root</a>/{{#each parents}}<a href="./{{path}}">{{name}}</a>/{{/each}}
        </h1>
        <p>Childrens:</p>
        <ul>
            {{#each childrens}}
            <li id="{{id}}"><a href="./{{path}}">{{name}}</a></li>
            {{/each}}
        </ul>
    </div>
    <div>
    <p>Components:</p>
    <ul>
        {{#each components}}
            <li id="{{id}}"><a href="#{{name}}">{{name}}</a></li>
        {{/each}}
    </ul>
    </div>
</body>
<script src="main.js"></script>
</html>